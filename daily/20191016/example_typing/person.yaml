components:
  schemas:
    Person:
      properties:
        name:
          type: string
        age:
          type: integer
        nickname:
          type: string
      required:
      - name
      - age
