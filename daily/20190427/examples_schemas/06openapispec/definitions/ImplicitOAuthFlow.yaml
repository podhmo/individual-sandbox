definitions:
  ImplicitOAuthFlow:
    type: object
    required:
    - authorizationUrl
    - scopes
    properties:
      authorizationUrl:
        type: string
        format: uriref
      refreshUrl:
        type: string
        format: uriref
      scopes:
        type: object
        additionalProperties:
          type: string
    patternProperties:
      ^x-: {}
    additionalProperties: false
