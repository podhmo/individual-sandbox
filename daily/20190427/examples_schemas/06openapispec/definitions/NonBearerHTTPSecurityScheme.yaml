definitions:
  NonBearerHTTPSecurityScheme:
    not:
      type: object
      properties:
        scheme:
          type: string
          enum:
          - bearer
    type: object
    required:
    - scheme
    - type
    properties:
      scheme:
        type: string
      description:
        type: string
      type:
        type: string
        enum:
        - http
    patternProperties:
      ^x-: {}
    additionalProperties: false
