x:
  definitions:
    group:
      type: object
      properties:
        name:
          $ref: "./primitive.yaml#/definitions/name"
        color:
          $ref: "./cmyk.yaml#/definitions/color"

y:
  definitions:
    group:
      type: object
      properties:
        name:
          $ref: "./primitive.yaml#/definitions/name"
        color:
          $ref: "./rgb.yaml#/definitions/color"
