## python jupyter emacs-keybindings

```
$ pip install jupyter-emacskeys
$ jupyter-nbextension list
Known nbextensions:
  config dir: /home/nao/.jupyter/nbconfig
    notebook section
      jupyter_emacskeys/init  enabled 
      - Validating: OK
```

firefoxだと `Ctrl + n` でwindowが開いちゃうので対応しないとだめ。
